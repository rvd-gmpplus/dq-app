export default function ExportsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Exports and backup</h1>
      <p className="mt-2 text-slate-600">JSON backup, CSV export, and the status report PDF.</p>
    </div>
  );
}
